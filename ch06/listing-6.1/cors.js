(function() {

  var start = function(res, resp, next) {
  };

  var allowOrigins = function(options) {
    return function(res, resp, next) {
    };
  };

  var allowCredentials = function(options) {
    return function(res, resp, next) {
    };
  };

  var allowMethods = function(options) {
    return function(res, resp, next) {
    };
  };

  var allowHeaders = function(options) {
    return function(res, resp, next) {
    };
  };

  var maxAge = function(options) {
    return function(res, resp, next) {
    };
  };

  var exposeHeaders = function(options) {
    return function(res, resp, next) {
    };
  };

  var end = function(res, resp, next) {
  };

  module.exports = {
    'configure': function(app) {
    };
  }
})();
